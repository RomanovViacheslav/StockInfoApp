import React from 'react';
import { Box } from '@mui/material';
import { Header, TableStock } from '../../modules';

const MainPage = () => (
  <Box width="100%">
    <Header />
    <TableStock />
  </Box>
);

export default MainPage;
